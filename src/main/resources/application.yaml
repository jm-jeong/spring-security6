
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eazybank
    username: root
    password: 1234
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: truelevel.org.springframework.security.web.SecurityFilterChain=Debug
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8080/realms/eazybankdev/protocol/openid-connect/certs

logging:
  level:
    org.springframework.security.web.SecurityFilterChain : debug


server:
  port: 8180